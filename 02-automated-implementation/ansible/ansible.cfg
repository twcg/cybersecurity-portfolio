[defaults]
inventory = ./inventories/production.ini
roles_path = ./roles
host_key_checking = False
private_key_file = ~/.ssh/portfolio_key
remote_user = lab
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_timeout = 30

